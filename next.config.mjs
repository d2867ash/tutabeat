import exp from 'constants';

/** @type {import('next').NextConfig} */
const nextConfig = {
// basePath:"/tutabeat",
//     output:"export",
//     reactStrictMode:true
};

export default nextConfig;
